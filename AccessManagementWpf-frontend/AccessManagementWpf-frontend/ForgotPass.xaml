<Window x:Class="AccessManagementWpf_frontend.ForgotPass"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AccessManagementWpf_frontend"
        WindowState="Maximized"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        mc:Ignorable="d"
        Title="ForgotPass" Height="450" Width="800">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <!-- Username Input Row -->
            <RowDefinition Height="Auto"/>
            <!-- Button Row -->
        </Grid.RowDefinitions>

        <Label Content="Enter username" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold"/>
        <!-- Username Input -->
        <TextBox x:Name="txtUsername" Height="50" Width="400"
             HorizontalAlignment="Center" VerticalContentAlignment="Center" 
             HorizontalContentAlignment="Center" FontSize="17" Margin="0,50,0,20" Grid.Row="0"/>

        <!-- Submit Button -->
        <Button x:Name="btnSubmit" Content="Submit" FontSize="16" Height="50" Width="180"
            HorizontalAlignment="Center" Click="btnSubmit_Click" 
            Grid.Row="1" Margin="0,20,0,0"/>
    </Grid>

</Window>
